# PixelRoot

## Overview
PixelRoot is a hardware-anchored media authenticity framework that embeds cryptographic provenance directly into image pixels at the moment of capture. By coupling CMOS sensor-level pixel modulation with real-time blockchain logging, PixelRoot enables tamper-evident verification of images and videos without relying on mutable metadata.

## Project Structure
- `index.js` - Main Node.js server (serves landing page on port 5000)
- `package.json` - Node.js project configuration
- `README.md` - Project description
- `PixelRoot.docx` - Detailed project documentation/specification
- `LICENSE` - Project license

## Running the Project
```bash
npm start
```
The server runs on port 5000.

## Technology Stack
- Node.js 20
- Plain HTML/CSS frontend

## Next Steps
The core PixelRoot framework (CMOS integration, blockchain logging, pixel-level provenance) needs to be implemented based on the documentation in `PixelRoot.docx`.
